{
  "name": "ccc-cards-game",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "types"
  ],
  "scripts": {
    "dev-server": "pnpm --filter server dev",
    "dev-client": "pnpm --filter client dev",
    "dev-client-electron": "pnpm --filter client dev-electron",
    "dev": "npm-run-all --parallel dev-server dev-client",
    "dev-electron": "npm-run-all --parallel dev-server dev-client-electron"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
